import 'package:flutter/material.dart';

class CreationProfilScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Créer un Profil'),
      ),
      body: Center(
        child: Text('Écran de création de profil.'),
      ),
    );
  }
}
